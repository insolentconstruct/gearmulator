--- dsp56300/gearmulator/source/xmemProbe/xmemProbe.cpp
+++ dsp56300/gearmulator/source/xmemProbe/xmemProbe.cpp
@@ -393,6 +393,12 @@
 		std::cout << "Using preset \"" << ROMFile::getSingleName(preset) << "\"" << std::endl;
 	}
 
+	std::cout << "Preset Dump:" << std::endl;
+	for (int i=0; i<512; i++) {
+		printf("%02x ", preset[i]);
+		if ((i+1) % 16 == 0) printf("\n");
+	}
+
 	DspSingle* dsp1raw = nullptr;
 	DspSingle* dsp2raw = nullptr;
 	Device::createDspInstances(rom, &dsp1raw, &dsp2raw);
